# 贡献指南

感谢参与 RAG Eval Platform。提交功能或修复前，请先确认变更属于公开、可复现的工程能力，且不会带入真实密钥、用户数据、内部地址或构建产物。

## 开发流程

1. 从 `main` 创建分支，并在变更前阅读 `AGENTS.md` 和相关契约文档。
2. 对新功能先补失败测试，再实现最小可用逻辑；修复缺陷必须补回归测试。
3. 运行 `make lint`、`make typecheck`、`make test`；涉及数据库或 Compose 时再运行对应集成检查。
4. 提交消息使用清晰的中文动词短语，说明公开行为变化；不要把临时调试内容混入提交。

## 契约与兼容性

OpenAPI 客户端通过 `pnpm generate:web-api` 生成。修改 API 后必须提交生成结果，并说明破坏性变化和迁移方式。跨模块数据格式必须更新 `docs/contracts/` 中的版本化契约。

## Pull Request

PR 描述应包含目标、边界、测试命令、已知缺口、安全/隐私影响和部署或迁移说明。外部模型调用不得出现在 CI；测试应使用本地 fixture 或 mock。
