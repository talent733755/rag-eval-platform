# RAG Eval Platform 开源工程规则

## 铁律

本项目必须按“完整的 GitHub 开源项目”建设，不能按一次性 Demo 或 PoC 的标准实现。任何后续代码、架构、依赖、接口和文档变更，都必须满足可公开使用、可复现、可维护、可测试、可审计和可持续演进的要求。

## 实施要求

### 架构与接口

- 模块边界、职责和依赖方向必须清晰，避免把业务逻辑耦合到某一家 LLM、Embedding、Reranker、向量数据库或 RAG 框架。
- Adapter、评测数据格式、Trace 格式和配置格式属于公共契约，必须显式定义、文档化并考虑版本兼容。
- 优先提供稳定的扩展点和替换机制；临时方案必须标明限制、替代路径和后续演进计划。
- 所有实验结果必须尽可能可复现：记录配置、数据集版本、模型/服务版本、随机性和运行环境。

### 代码质量

- 新功能必须包含与风险匹配的自动化测试；修复缺陷必须补充回归测试。
- 代码应具备明确的错误处理、输入校验、超时、重试、取消和资源释放策略。
- 关键流程必须有结构化日志、错误上下文和可观测性，不能只依赖 `print` 或吞掉异常。
- 禁止硬编码密钥、Token、个人信息、内部地址和机器相关路径；敏感配置必须通过安全的配置方式注入。
- 不引入无法说明用途、许可证或维护状态的依赖；依赖升级需要关注兼容性和安全公告。

### 开源协作

- 面向用户的行为、配置、接口和限制必须写入文档，并提供最小可运行示例。
- 公开变更需要有清晰的提交、变更记录和版本策略；破坏性变更必须明确标注迁移方式。
- 项目应逐步完善 `CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、`CHANGELOG.md`、许可证文件、Issue/PR 模板和 CI 工作流。
- 不提交构建产物、临时文件、用户数据、测试密钥或无法公开的业务材料。

## 完成标准

除非用户明确说明只是探索性原型，否则一个功能只有在以下内容齐备或明确记录缺口后，才能视为完成：

1. 设计目标、边界和公共接口已文档化。
2. 核心逻辑、异常路径和兼容性已有测试。
3. 本地质量检查、测试和构建已执行并记录结果。
4. 安全、隐私、依赖许可证和运行成本风险已检查。
5. 使用示例、配置说明和必要的迁移说明已更新。

如果当前仓库缺少实现基础设施，应先补齐项目骨架和质量门禁，再继续堆叠业务功能。
