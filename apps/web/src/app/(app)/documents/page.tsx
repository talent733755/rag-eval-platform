import { EmptyState } from "@/components/ui/empty-state";

export default function DocumentsPage() {
  return <EmptyState title="文档库" description="文档库未来负责管理原始资料、版本和可追溯的解析来源。业务能力将在后续计划中接入。" actionLabel="返回工作台" actionHref="/" />;
}
