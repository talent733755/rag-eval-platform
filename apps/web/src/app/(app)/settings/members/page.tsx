import { EmptyState } from "@/components/ui/empty-state";

export default function MembersPage() {
  return <EmptyState title="项目与成员" description="项目与成员未来负责管理可见项目、成员角色、访问权限和管理操作审计。" actionLabel="返回工作台" actionHref="/" />;
}
