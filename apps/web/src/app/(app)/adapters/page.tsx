import { EmptyState } from "@/components/ui/empty-state";

export default function AdaptersPage() {
  return <EmptyState title="Pipeline 接入" description="Pipeline 接入未来负责配置外部 RAG Pipeline Adapter 和稳定的执行契约。" actionLabel="返回工作台" actionHref="/" />;
}
